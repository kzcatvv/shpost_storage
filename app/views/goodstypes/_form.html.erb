<%= form_for @goodstype, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="form-group">
    <%= f.label :gtno, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :gtno, :class => 'form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :unit_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :unit_id, :class => 'form-control' %>
    </div>
  </div>
  
  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            goodstypes_path, :class => 'btn btn-default' %>
 
<% end %>
