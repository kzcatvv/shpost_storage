<%= form_for(@mobile_log) do |f| %>
  <% if @mobile_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mobile_log.errors.count, "error") %> prohibited this mobile_log from being saved:</h2>

      <ul>
      <% @mobile_log.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
